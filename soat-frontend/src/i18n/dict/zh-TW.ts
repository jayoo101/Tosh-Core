import type { PartialDictionary } from '../types'

/**
 * 繁體中文 — 尚未翻譯。
 *
 * ⚠ 空的字典是可用狀態,不是損壞狀態。每個缺少的 key 都會逐條回退英文,所以
 *   這個檔案存在的意義是「翻譯要放在這裡」,而不是「這個語言壞了」。
 *   `NEXT_PUBLIC_LOCALES` 不列出 zh-TW 時,它連載入都不會發生。
 *
 * ⚠ 不要從 `zh-CN.ts` 做簡繁轉換就當完成。用詞差異不只在字形:
 *   「区块链」/「區塊鏈」只是字形,但「默认」對「預設」、「信息」對「訊息」、
 *   「登录」對「登入」是不同的詞。金額與退款這類文案照抄轉換的結果,讀起來
 *   會是一份machine-translated 的公告,而這是最不該讓人起疑的地方。
 *
 * 開始翻譯時,請把 `zh-CN.ts` 的註解一起讀 —— 那裡記錄了每一句為什麼那樣寫。
 */
export const ZH_TW: PartialDictionary = {}
